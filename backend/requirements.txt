# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Database
sqlalchemy==2.0.30
alembic==1.13.1
aiosqlite==0.20.0        # async SQLite driver

# HTTP client (async)
httpx==0.27.0

# Scheduling (no Redis needed for personal use)
apscheduler==3.10.4

# Data & analytics
pandas==2.2.2
numpy==1.26.4

# Config & validation
pydantic==2.7.1
pydantic-settings==2.2.1
python-dotenv==1.0.1

# Auth & security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Date/time
python-dateutil==2.9.0.post0

# FIT file parsing (for manual Garmin fallback if needed later)
fitparse==1.2.0

# Testing
pytest==8.2.0
pytest-asyncio==0.23.6
httpx==0.27.0            # also used as test client
